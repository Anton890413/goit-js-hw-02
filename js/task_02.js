const calculateEngravingPrice = function (message, pricePerWord = 0) {
  pricePerWord <= calculateEngravingPrice;
  message = message.split([" "]);
  message = message.length;
  pricePerWord;
  const result = message * pricePerWord;
  if (message === "" && pricePerWord === undefined) {
    result = 0;
  }
  return result;
};

const firstTask = calculateEngravingPrice("", undefined);

console.log(firstTask);
